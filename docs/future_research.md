# Future Research

- Complete real-model experiments in both sibling benchmarks (see each
  repo's own Roadmap) — the actual blocker for everything else here.
- SentinelAI Version 2: validated multilingual refusal classifier +
  larger sample, to move the cross-lingual evidence from "pilot, caveated"
  to "confirmatory."
- Extend the evidence-pull mechanism (`scripts/pull_evidence.py`) to a
  third benchmark if/when one exists, keeping the same
  mock-data safety gate.
- Once AgentGuard (guardrail implementation project) exists, add a
  section connecting policy recommendations here to concrete
  implementation patterns there — closing the loop from evidence to
  engineering.
