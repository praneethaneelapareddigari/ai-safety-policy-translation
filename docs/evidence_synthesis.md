# Cross-Project Evidence Synthesis

> ⚠️ **Status: waiting on real experimental data.** The tables below are
> generated by `scripts/pull_evidence.py`, which reads results directly
> from the sibling repos. It refuses to render `technical-ai-safety-experiment`
> data tagged `data_provenance: SIMULATED` as evidence (see that script's
> safety gate) — so until Experiment 03 (real-model runs) completes there,
> this section legitimately has nothing to show for that benchmark.
> SentinelAI's data is real but pilot-scale (28 prompts) with known
> classifier caveats, both attached automatically below.

## From Prompt Injection & Jailbreak Robustness Benchmark

To regenerate this section once real-model results exist:

```bash
python scripts/pull_evidence.py --source tase \
  --path ../technical-ai-safety-experiment/results/tables/summary.json \
  --out evidence/tase_evidence.md
```

<!-- INSERT: evidence/tase_evidence.md once it contains REAL (non-mock) data -->
*(pending real-model experiment run — see that repo's Roadmap, Experiment 03)*

## From Cross-Lingual LLM Safety Evaluation (SentinelAI)

To regenerate this section:

```bash
python scripts/pull_evidence.py --source sentinelai \
  --path ../SentinelAI/results/csv/refusal_rate.csv \
  --out evidence/sentinelai_evidence.md
```

<!-- INSERT: evidence/sentinelai_evidence.md -->
*(Pilot v1 data exists and is real, but is pilot-scale with confirmed
classifier caveats — see the auto-attached caveats in the generated file.
Treat as illustrative of the measurement pipeline, not a confirmed
cross-lingual finding, until SentinelAI's Version 2 — expanded classifier
+ larger sample — is complete.)*

## Synthesis (do not write until both sections above have real content)

This section is intentionally left unwritten. Drawing a cross-project
narrative from a blocked/pending evidence table and a caveated 28-prompt
pilot would be exactly the "opinion dressed as evidence" failure mode
this project exists to avoid.
